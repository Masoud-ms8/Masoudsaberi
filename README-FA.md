# 🧭 پروژه گردشگری با جنگو (Tourism Django Project)

## 📌 معرفی پروژه

این پروژه یک **بک‌اند گردشگری** است که با استفاده از **Django** توسعه داده شده است.  
هدف اصلی پروژه، تمرین و تسلط بر مفاهیم **معماری بک‌اند، مدل‌سازی داده، طراحی API و تست‌نویسی** بوده است.

پروژه در ابتدا با استفاده از **Django REST Framework (DRF)** طراحی شد و سپس در ادامه، به‌صورت کامل **بازطراحی و مهاجرت داده شد به GraphQL** با استفاده از **Graphene-Django** تا امکان کوئری‌نویسی منعطف‌تر فراهم شود.

این اپلیکیشن مدیریت می‌کند:
- کاربران
- مکان‌های گردشگری
- برنامه‌های سفر

---

## 🧠 روند تکامل پروژه (مهم)

این پروژه در **دو فاز اصلی** توسعه داده شده است:

### فاز اول – REST API (DRF)
- پیاده‌سازی APIهای RESTful برای تمام منابع
- پشتیبانی کامل از عملیات CRUD
- استفاده از DRF Browsable API برای تست دستی

### فاز دوم – مهاجرت به GraphQL (وضعیت نهایی)
- جایگزینی کامل REST API با **GraphQL**
- پیاده‌سازی Query و Mutationها
- ایجاد یک Schema مرکزی برای GraphQL
- نوشتن تست‌های خودکار برای APIهای GraphQL
- اندازه‌گیری و بررسی میزان پوشش تست‌ها (Coverage)

➡️ **نسخه نهایی و فعلی پروژه بر پایه GraphQL است.**

---

## 🏗️ تکنولوژی‌های استفاده‌شده

- Python 3
- Django
- Graphene-Django (GraphQL)
- Django REST Framework (در فاز اولیه)
- SQLite (دیتابیس محیط توسعه)
- Django Test Framework
- Coverage.py

---

## 📁 ساختار پروژه

```

tourism_project/
├── accounts/        # مدیریت کاربران
├── destinations/   # مکان‌های گردشگری
├── plans/           # برنامه‌های سفر
├── config/          # تنظیمات کلی و GraphQL schema
└── manage.py

````

---

## 👤 اپلیکیشن Accounts

### هدف
مدیریت کاربران سیستم.

### مدل
- استفاده از مدل پیش‌فرض `User` در Django

### GraphQL
- Query:
  - `allUsers`
- Mutation:
  - `createUser`

### نمونه Query
```graphql
query {
  allUsers {
    id
    username
    email
  }
}
````

---

## 🏨 اپلیکیشن Destinations

### هدف

مدیریت مکان‌های گردشگری.

### مدل

فیلدهای Destination:

* name
* city
* description
* price

### GraphQL

* Query:

  * `allDestinations`
* Mutation:

  * `createDestination`

### نمونه Mutation

```graphql
mutation {
  createDestination(
    name: "Persepolis"
    city: "Shiraz"
    description: "Historical site"
    price: 0
  ) {
    destination {
      id
      name
      city
      price
    }
  }
}
```

---

## 🗺️ اپلیکیشن Plans

### هدف

مدیریت برنامه‌های سفر و ارتباط بین داده‌ها.

### مدل

هر برنامه سفر:

* متعلق به یک کاربر است
* می‌تواند شامل چندین مقصد باشد (Many-to-Many)
* دارای عنوان و توضیحات است

### GraphQL

* Query:

  * `allPlans`
* Mutation:

  * `createPlan`

### نمونه Query

```graphql
query {
  allPlans {
    title
    user {
      username
    }
    destinations {
      name
      city
    }
  }
}
```

---

## 🔗 ارتباط بین داده‌ها

* هر **User** می‌تواند چندین **Plan** داشته باشد
* هر **Plan** می‌تواند شامل چندین **Destination** باشد
* تمام این ارتباط‌ها به‌طور کامل از طریق GraphQL تست شده‌اند

---

## 🧪 تست‌نویسی

برای هر اپلیکیشن تست‌های GraphQL نوشته شده است:

* accounts
* destinations
* plans

### مواردی که تست شده‌اند:

* در دسترس بودن endpoint گراف‌کیوال
* صحت اجرای Queryها
* ساختار داده‌های برگشتی
* عدم وجود خطا در اجرای درخواست‌ها

### اجرای تست‌ها

```bash
python manage.py test
```

---

## 📊 پوشش تست (Test Coverage)

برای اندازه‌گیری پوشش تست‌ها از **coverage.py** استفاده شده است.

```bash
coverage run manage.py test
coverage report
```

* میزان پوشش تست کلی **بیش از ۸۰٪** است
* الزامات پروژه را به‌طور کامل برآورده می‌کند

---

## 🧹 پاک‌سازی دیتابیس در طول توسعه

در حین توسعه، داده‌های تستی متعددی به‌صورت دستی ایجاد شد.
پیش از تست نهایی:

* دیتابیس به‌طور کامل پاک‌سازی شد
* داده‌ها به‌شکل کنترل‌شده و مرتبط دوباره ساخته شدند
* تمام ارتباط‌ها مجدداً از طریق GraphQL تست شدند

---

## ✅ وضعیت نهایی پروژه

* ✔️ مدل‌های داده‌ای صحیح
* ✔️ پیاده‌سازی کامل Query و Mutationهای GraphQL
* ✔️ ساختار تمیز و قابل‌فهم Schema
* ✔️ تست‌های خودکار
* ✔️ پوشش تست بالاتر از حد موردنیاز

---

## 📝 توضیحات تکمیلی

این پروژه با هدف **یادگیری مفاهیم بک‌اند** توسعه داده شده و برای محیط Production در نظر گرفته نشده است.
تمرکز اصلی پروژه بر موارد زیر بوده است:

* صحت پیاده‌سازی
* ساختار مناسب
* ارتباط بین داده‌ها
* تست‌نویسی

---