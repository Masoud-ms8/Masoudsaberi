# 🧭 Tourism Project – Django + GraphQL

این پروژه یک بک‌اند ساده ولی ساخت‌یافته برای مدیریت **کاربران، مقاصد گردشگری و برنامه‌های سفر** است که با استفاده از **Django** و **GraphQL** پیاده‌سازی شده و دارای **تست‌نویسی با پوشش بالا (Coverage)** می‌باشد.

هدف پروژه، تمرین عملی مفاهیم زیر بوده است:
- طراحی مدل‌های مرتبط
- پیاده‌سازی GraphQL API
- نوشتن Query و Mutation
- تست‌نویسی اصولی و قابل دفاع

---

## 📌 تکنولوژی‌های استفاده‌شده

- Python 3
- Django
- Graphene-Django (GraphQL)
- SQLite (برای محیط توسعه)
- Django Test Framework
- Coverage.py

---

## 🏗️ ساختار کلی پروژه

پروژه شامل سه اپلیکیشن اصلی است:

```

tourism_project/
├── accounts/        # مدیریت کاربران
├── destinations/   # مدیریت مقاصد گردشگری
├── plans/           # مدیریت برنامه‌های سفر
├── config/          # تنظیمات اصلی پروژه و schema کلی GraphQL
└── manage.py

````

---

## 👤 Accounts (کاربران)

### مدل
از مدل پیش‌فرض `User` در Django استفاده شده است.

### GraphQL
- Query:
  - `allUsers`
- Mutation:
  - `createUser`

### مثال Query
```graphql
query {
  allUsers {
    id
    username
    email
  }
}
````

---

## 🏨 Destinations (مقاصد گردشگری)

### مدل

هر مقصد شامل اطلاعات زیر است:

* name
* city
* description
* price

### GraphQL

* Query:

  * `allDestinations`
* Mutation:

  * `createDestination`

### مثال Mutation

```graphql
mutation {
  createDestination(
    name: "برج میلاد"
    city: "تهران"
    description: "نماد تهران"
    price: 0
  ) {
    destination {
      id
      name
      city
      price
    }
  }
}
```

---

## 🗺️ Plans (برنامه‌های سفر)

### مدل

هر برنامه سفر:

* متعلق به یک کاربر است
* می‌تواند چند مقصد داشته باشد (ManyToMany)
* دارای عنوان و توضیح است

### GraphQL

* Query:

  * `allPlans`
* Mutation:

  * `createPlan`

### مثال Query

```graphql
query {
  allPlans {
    title
    user {
      username
    }
    destinations {
      name
      city
    }
  }
}
```

---

## 🧪 تست‌نویسی (Testing)

برای هر اپلیکیشن، تست‌های جداگانه GraphQL نوشته شده است.

ویژگی‌های تست‌ها:

* استفاده از Django Test Client
* ارسال درخواست POST به `/graphql/`
* بررسی:

  * status code
  * نبودن error
  * صحت داده‌های برگشتی

### اجرای تست‌ها

```bash
python manage.py test
```

### اجرای Coverage

```bash
coverage run manage.py test
coverage report
```

### نتیجه Coverage

پوشش تست‌ها بالاتر از **80٪** بوده و الزامات پروژه را پوشش می‌دهد.

---

## 🧹 پاک‌سازی دیتابیس در زمان توسعه

در طول توسعه، داده‌های تستی زیادی ایجاد شد.
برای تست نهایی:

* دیتابیس پاک‌سازی شد
* داده‌ها به‌صورت کنترل‌شده و مرتبط دوباره ساخته شدند
* صحت ارتباط‌ها از طریق GraphQL بررسی شد

---

## ✅ وضعیت نهایی پروژه

* ✔️ مدل‌ها کامل و مرتبط
* ✔️ Query و Mutation‌ها فعال
* ✔️ GraphQL API سالم
* ✔️ تست‌نویسی انجام‌شده
* ✔️ Coverage بالاتر از حد موردنیاز

---

## 📎 توضیح پایانی

این پروژه با تمرکز بر **درک مفاهیم بک‌اند، GraphQL و تست‌نویسی** انجام شده و هدف آن پیاده‌سازی یک سیستم پیچیده نبوده، بلکه ساخت یک پایه‌ی صحیح و قابل توسعه بوده است.